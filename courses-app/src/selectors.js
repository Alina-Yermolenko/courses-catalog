export const getStoreCourses = (state) => state.courses;
export const renderedAuthors = (state) => state.authors;
export const getAuthorsReducer = (state) => state.authorsReducer;
export const getStoreUser = (state) => state.user;
